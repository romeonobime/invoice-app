{% props value = "", model = "", labelName = "" %}

<twig:Base:Label
    class="w-fit cursor-pointer flex items-center gap-3"
>
    <twig:Base:Checkbox
        value="{{ value }}"
        model="{{ model }}"
    />
    {{ labelName }}
</twig:Base:Label>
