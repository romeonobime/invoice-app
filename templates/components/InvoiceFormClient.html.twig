<div class="flex flex-col gap-6">
    {{ form_label(form, null, { 'label_attr': {'class': 'heading-small-variant text-primary'} }) }}
    {{ form_row(form.name) }}
    {{ form_row(form.email) }}
    <twig:InvoiceFormAddress form="{{ form.address }}" />
</div>
