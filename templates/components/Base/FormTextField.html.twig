{% props labelName = "", placeholder = "" %}

<twig:Base:Label theme="lighter" size="small" class="flex flex-col gap-2">
    {{ labelName }}
    <input
        type="text"
        placeholder="{{ placeholder }}"
        class="font-bold text-neutral-darkest body-variant h-12 bg-primary/0 p-5 border border-neutral-lightest rounded focus:border-primary" 
    />
</twig:Base:Label>
